{
  "buildCommand": "npx expo export --platform web --output-dir dist && node scripts/postbuild.js && cp -r public/* dist/",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
